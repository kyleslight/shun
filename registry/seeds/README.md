# First-party seed packages

Packages committed here are published by `pnpm registry:catalog` as first-party
entries. They are ordinary packages: the same validator and the same packer the
application uses, so nothing here can be true of a seed that is not also true of
a community upload.

Third-party plugins do not belong in this directory — they are published by their
authors through the registry's publish API.
